export default function SetupPage() {
  return (
    <div className="grid min-h-dynamic-screen place-items-center p-8 text-center">
      <div>
        <h1 className="font-display text-4xl font-bold">POS — No branches yet</h1>
        <p className="mt-3 text-lg text-muted-foreground">
          Create a branch in the Admin app, then open this on your tablet.
        </p>
      </div>
    </div>
  );
}
